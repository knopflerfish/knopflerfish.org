Utility program for changing package and directory name. 

Also supports prefixing source files with new comment section.

Usage: 

java -jar pkgname.jar \
   [-renamepkg dir oldname newname] \
   [-addcopyright dir prefixfile] \
   [-removecomments dir]
