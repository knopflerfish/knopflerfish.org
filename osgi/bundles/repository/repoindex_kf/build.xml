<?xml version="1.0" encoding="UTF-8"?>

<project name="repoindex_kf" default="all">

  <dirname  property="proj.dir" file="${ant.file.repository}"/>
  <property name="topdir"       location="${proj.dir}/../../.."/>

   <property name="impl.pattern"
	    value="org/knopflerfish/bundle/**"/>

  <property name = "bundle.build.lib"   value = "false"/>
  <property name = "bundle.build.api"   value = "false"/>
  <property name = "bundle.build.impl"  value = "false"/>
  <property name = "bundle.build.all"   value = "true"/>

  <!-- Build for OSGi Minimum Execution Environment. -->
  <property name="bundle.compile.EE" value="ee.minimum"/>

  <property name="do.bundle.custom.pre" value="true"/>
  <target name="bundle.custom.pre">

    <!-- Embedd APIs for OSGi services that the desktop depends on -->
    <bundle_locator bundleNames="org.osgi.impl.bundle.repoindex.api">
      <fileset dir="${proj.dir}">
        <exclude name="**/*-source.jar"/>
        <exclude name="**/*-javadoc.jar"/>
        <include name="**/*.jar"/>
      </fileset>
    </bundle_locator>
    <copy todir="${classes.out.dir}">
      <zipfileset src="${bap.org.osgi.impl.bundle.repoindex.api}">
        <patternset>
          <exclude name="META-INF/**/*"/>
          <exclude name="**/*.dex"/>
        </patternset>
      </zipfileset>
    </copy>
  </target>

  <path id="bundle.compile.path">
  </path>

  <import file="${topdir}/../ant/bundlebuild.xml"/>

</project>
